#include<bits/stdc++.h>
using namespace std;
int fact(int n)
{
    int ft = 1;
    for(int i=2;i<=n;i++)
        ft*=i;
    return ft;
}

int main()
{
    int n,r;
    cout<<"enter two number = ";
    cin>>n>>r;
    cout<<"permutation = ";
    cout<<(fact(n)/(fact(r)*fact(n-r)));
}
