#include<bits/stdc++.h>
using namespace std;
int fact(int n)
{
    int ft = 1;
    for(int i=2;i<=n;i++)
        ft*=i;
    return ft;
}
int main()
{
	int num;
	cout<<"Enter a number = ";
	cin>>num;
	cout<<"factorial = "<<fact(num); 
}
